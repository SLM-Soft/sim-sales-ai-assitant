# AWS Configuration for Netlify Functions
# Copy this file to .env and fill in your actual AWS credentials

AWS_REGION=eu-central-1
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
BEDROCK_MODEL_ID=amazon.titan-text-express-v1

# Optional: Enable mock mode for development without AWS credentials
# Uncomment the line below to return mock responses instead of calling AWS Bedrock
# USE_MOCK_BEDROCK=true

# Note: Do NOT set VITE_API_URL for Netlify Dev
# The default '/api' path works with Netlify's redirect rules
